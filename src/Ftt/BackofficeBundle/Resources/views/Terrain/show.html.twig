{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Terrain</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Nomterrain</th>
                <td>{{ entity.nomTerrain }}</td>
            </tr>
            <tr>
                <th>Adresseterrain</th>
                <td>{{ entity.adresseTerrain }}</td>
            </tr>
            <tr>
                <th>Capacite</th>
                <td>{{ entity.capacite }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('terrain') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('terrain_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
